# REPORT — ماذا قال المستخدم فعلاً (بأرقام)

المصدر: `logs/answers.log` — السجل الخام لموقع القرارات. ملف `answers_seed.js`
ناتج ثانوي (via `make_seed.py`) وُجد ناقصاً: يحمل 116 مفتاحاً ويسقط `c05` (سؤال
غير معرّف في صفحة الموقع) ولا يحفظ نص السؤال — فاعتُمد السجل أصلاً والبذور شاهداً.

## الأرقام

- 118 سطراً في السجل = 1 اختبار حلقة (TEST) + 117 سؤالاً حقيقياً.
- 117 سؤالاً حقيقياً ← 98 مُجابة (84%) + 7 مؤجلة بقرار صريح + 12 غير مُجابة.
- أنواع الأجوبة: 64 ملاحظة حرة، 44 اختياراً من قائمة، 6 تقييمات /10، 4 ترتيبات.
- الاستخراج أنتج: 46 التزاماً تشغيلياً + 9 حقول أولويات/نموذج + 8 تناقضات
  محلولة صراحة — كل استنتاج يحمل evidence=معرّفات أسئلة.
- الحالة المبدئية: 0 ذرة متقنة، 5 ذرات مفتوحة (جذور السلاسل)، 30827 ذرة مقفلة
  من أصل 30832 في الكوربوس.

## غير المُجاب عنها (تُخطّت — لم تُخترع)

`d05 d07 p08 p11 w01 w13 b07 t03 n03 n05 i05 l12` — لم يفهم السؤال، أو «لا أعرف»،
أو اختار placeholder حرفياً، أو نسي المشاريع المشار إليها.

## المؤجلة بقراره هو (لا تُقرأ كإجابة)

`d13 s08 b01 b09 g10 g17 i10` — «لم أحسمه»، «سنختار لاحقاً»، «جلسة عميقة»،
«اكتب لي المؤلفين أولاً». تُحفظ قائمةً مفتوحة لا حقائق.

## التناقضات الثمانية وكيف حُلّت

1. `l06` — السؤال يفترض إكماله كورسات جاد الأربعة؛ صحّح بنفسه: ~10% من الكورس
   الأول فقط. ← اعتُمد تصحيحه: لا إتقان برمجي سابق.
2. `d09` مقابل `l01` — «إنتاج الدروس» قبل «تعلم البرمجة» مقابل «العلم الشرعي»
   أولاً. ← محوران (إنتاج/تعلم) لا تناقض؛ الشرعي أولاً في كليهما.
3. `w10` مقابل `t01`+`d04` — ثقة 3/10 بالوكلاء مقابل تفويض تقني واسع.
   ← تفويض مراقَب: بطاقة تجربة سريعة + مبادرة بإشعار (w09, w02).
4. `p07` مقابل `d04` — رفض التمارين المولّدة مقابل قبول أسئلة مولّدة.
   ← الافتراضي منتقاة خبيراً؛ المولّد «إضافي» علاجي فقط.
5. `c10`+`l11` مقابل `s05` — «صفر تقدم» و«لا أجد شيئاً» مقابل «سنوات من
   المتابعة». ← التعرّض السماعي ليس إتقان ذرة؛ mastered=[] والإتقان بالتقييم.
6. `b05` مقابل `g04` — رفض ترتيب تغذية المنصة مقابل ترتيب العلوم المعلن.
   ← G04 اعتُمد لخطة الدراسة لا لأولوية التغذية.
7. `g15`+`l07` — فلسفة الإنجليزية «قريبة لكن ناقصة». ← اعتُمدت تقريباً مع
   علم مفتوح «التصحيح يكتبه لاحقاً».
8. `n01`+`b03`+`b04` مقابل آلية القص — يقبل الفهرس الخاص ويخشى كونه أصل خطأ.
   ← فهرس مخصص لكل مصدر يُسقط على العام + اعتماد بشري واحداً واحداً (g05).

## الحقيقة الجوهرية المستخرجة

- الأولويات: شرعي › برمجة › إنجليزية › علوم أخرى (l01)؛ الشرعي 8/10 (s07)؛
  الوقت اليومي 9/10 (l04).
- نموذجه المثبت: مصطفى السعد — معلومة صغيرة + تمارين خبير متدرجة + جهد قبل الحل
  + قائمة صعب جانبية + جدول مراجعة (l05).
- الالتزام الكبير: FSRS على الذرات **والمهارات** معاً — نجاح←أبعد وأصعب،
  فشل←أقرب وأسهل، تجربة جانبية أولاً (l08, g13, d04).
- الشيخان المفضلان: محمد البردوني الحسيمي ومولود السريري (s05, g06) — **السريري
  غير موجود في كوربوس atoms الحالي**: فجوة بيانات موثقة في `start_plan.md`.
- مشكلته المعلنة: النسيان أولاً، ثم كره اختراع مشاريع خاصة، ثم تضييع الوقت في
  التفاصيل — وليست إرادة (l03, l11, d10).
- الوكيل عنده: شريك يناقش «لماذا» قبل «كيف»، لا كاتب كود مسرع (w11).

## البرهان على خطة البدء

خمس سلاسل بترتيب علومه المعلن (g04) وشيخه المفضل (s05/g06):
أم البراهين (عقيدة) ← شرح الرسالة (فقه) ← المنهج المنتخب (أصول) ← الحكم العطائية
(تزكية) ← تفسير السعدي (تفسير — اختيار افتراضي، لم يعلن مفضلاً للتفسير).
التفصيل في `start_plan.md`، والذرات المفتوحة/المقفلة في `seed_state.json`.
