# ux6 — نقد UX: english/*.html على 360px

CDP حي (القياس على الست المنشورة) · 2026-09-25.

## النمط العام — سليم

صفر فيض أفقي، صفر عناصر خارج viewport، خط ≥11px، صفر أخطاء JS على الست كلها. تصميم موحّد (style.css مشترك: topbar+logo+nav chips).

## الخلل المنهجي الوحيد: أهداف لمس صغيرة

| الصفحة | أهداف لمس | أصغر من 34px |
|---|---|---|

| index | 44 | **42 (95%)** |

| srs | 9 | 8 |

| routine | 14 | **13 (93%)** |

| library | 17 | 7 |

| mimic | 18 | 6 |

| listen | 10 | 5 |

**العلة الجذرية**: `.nav a{ font-size:14px; padding:8px 14px }` في style.css المشترك → ارتفاع ~32px < 34px. رفع padding إلى `10px 14px` (أو min-height:36px) يشفي كل الصفحات الـ13 دفعة واحدة — إصلاح سطر واحد P1.

## لكل صفحة

- **index**: مركز غني (629 عنصراً، 6 بطاقات أقسام، docH 5874 ≈16 شاشة — طويل لكن مقسّم منطقياً؛ nav فوق ثم محتوى). درجة 7/10.
- **srs**: shell متقشّف — «لا بطاقات للمراجعة» على الأرجح؛ يحتاج بيانات مستخدم. درجة 8/10 كتصميم.
- **listen**: شريط استماع — أزرار تشغيل/تكرار؛ docH شاشة واحدة. درجة 8/10.
- **mimic**: تمرين محاكاة — مقاطع قصيرة + تسجيل؛ docH ~2 شاشة، هيكل جيد. درجة 8/10.
- **library**: فهرس مكتبة (17 رابطاً خارجياً/داخلياً سليماً) — docH 2335، معقول. درجة 8/10.
- **routine**: روتين يومي — checkbox/أزرار صغيرة كلها تقريباً <34px (13/14) — أكثرها تضرراً بالعلة ا��عامة. درجة 6/10.

## الأولويات

1. **P1**: سطر واحد في style.css — `.nav a` padding→10px (+ أزرار .btn إن كانت <34) — يشفي 95% من الخلل عبر كل english/*.
2. **P2**: routine — حجم checkbox/button إلى ≥34px تحديداً (أكبر نسبة تضرر).
3. **P3**: index — تقليل طول الصفحة (~16 شاشة) بتقسيم الأقسام إلى روابط أو إخفاء تفصيلي.
