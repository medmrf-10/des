# REPORT_diff — أثر استهلاك annotations.json على SKILL_GRAPH

التغيير: `build_graph.py` يحمّل `jadid2/atoms/<series>/annotations.json` بجانب كل
فهرس (إن وُجد) فيستعمل `kind`/`main_tag`/`sub_tags`/`difficulty` الحقيقية بدل
الاشتقاق؛ السلاسل بلا ملف جانبي تبقى على الاشتقاق كما كان.

**الخطان المقارَنان** (نفس الكوربوس الحالي: 43 سلسلة / 752 ملفاً / 32510 ذرة):

- **قبل** = الكود القديم بلا annotations (اشتقاق كامل).
- **بعد** = annotations مفعّلة (37 سلسلة / 5838 ذرة مصنّفة بالكامل).

ملاحظة مرجعية: آخر `graph.edges.json` مودع (5492 حافة) بُني على كوربوس أقدم
(35 سلسلة / 5254 ذرة) — قد أُضيفت منذئذ 6 سلاسل كبرى (4 تفاسير + دليل الفالحين +
جامع العلوم) غير مصنّفة، فتعمل على الاشتقاق.

## الحواف بكل قاعدة — قبل/بعد

| القاعدة | خام قبل | خام بعد | نهائية قبل | نهائية بعد | Δ نهائية |
|---|---|---|---|---|---|
| `manual` (R6) | 0 | 0 | 0 | 0 | 0 |
| `matn_sharh` (R2) | 398 | 398 | 398 | 398 | 0 |
| `sequence` (R1) | 31758 | 31758 | 31371 | 31371 | 0 |
| `kind_anchor` (R5) | 0 | 6 | 0 | 1 | **+1** |
| `sequence_cross` (R3) | 709 | 709 | 698 | 698 | 0 |
| `tag_chain` (R4) | 51433 | 51871 | 26953 | 27236 | **+283** |
| **المجموع** | | | **59420** | **59704** | **+284** |
| دورات مكسورة (Kahn) | | | **0** | **154** | +154 |

## R5 kind_anchor — تفعيل فعلي

`kind` صار معلناً لـ5838 ذرة (5820 معلومة / **18 مهارة** في 767 و787 و797).
تطبيق المواصفة حرفياً (مهارة ← كل معلومة في نفس الملف بنفس `sub_tag`) أنتج
6 أزواج خام، مصيرها:

| الحافة (معلومة → مهارة) | المصير |
|---|---|
| `767|002.08 → 002.09` | ازدواج: نفس الزوج حافة `sequence` (1.0 > 0.8) — بقيت sequence |
| `767|009.04 → 009.05` | ازدواج مع sequence |
| `767|009.06 → 009.05` | **أسقطها Kahn** — المعلومة تلي المهارة في الملف ⇒ حلقة مع مسار التسلسل |
| `767|020.02 → 020.03` | ازدواج مع sequence |
| `767|022.02 → 022.04` | **نجت كـ kind_anchor** — معلومة غير مجاورة تقيّد المهارة |
| `767|022.03 → 022.04` | ازدواج مع sequence |

النتيجة: **1 حافة kind_anchor نهائية** (من 0). القلة بنيوية: وسوم sub_tag للمهارة
غالباً تطبيقية («تطبيق»، «استخراج_الأفعال») لا تتشارك مع معلومات الملف إلا نادراً.

## R4 tag_chain — هل تغيّرت الكثافة بالـdifficulty الحقيقية؟

| المقياس | قبل (مشتق) | بعد (حقيقي) | Δ |
|---|---|---|---|
| مجموعات (main_tag, sub_tag مُطبَّعة) متعددة السلاسل | 218 | 355 | +137 |
| العضويات | 52599 | 53941 | +1342 |
| حواف خام | 51433 | 51871 | +438 |
| حواف نهائية | 26953 | 27236 | **+283 (+1.05%)** |
| ثقة 0.7 (تطابق حرفي) | 51348 | 51863 | +515 |
| ثقة 0.5 (بعد التطبيع فقط) | 85 | 8 | −77 |

الكثافة ارتفعت طفيفاً. السببان:

1. **sub_tags الحقيقية** أوسع تقاطعاً بين السلاسل من `tags[1:]` المشتقة
   (مفردات التصنيف الموحدة مثل «إعراب»/«عقيدة») ⇒ مجموعات أكبر (+137).
2. **difficulty الحقيقية (1–5)** أعادت ترتيب داخل المجموعات: كانت المشتقة
   مونوتونية على رتبة السلسلة (لا تعادل أبداً)، والآن التعادلات كثيرة فالقاعدة
   تتخطاها للأدنى الصارم — وأكدت تطابق sub_tag حرفياً أكثر (0.7: +515).

## الدورات

- قبل: **0** — الـdifficulty المشتق مونوتوني بنيوياً.
- بعد: **154 حافة أسقطها Kahn** (`cycles.log`): 153 `tag_chain` + 1 `kind_anchor`.
  السبب: الـdifficulty الحقيقية لا توافق ترتيب المواضع دائماً (ذرة «أصعب» قد تسبق
  «أسهل» داخل المنهج)، وkind_anchor يربط للخلف عندما تسبق المهارة معلومتها —
  أضعف حافة (أدنى confidence) تُسقط حتى انجلاء الدورات.

## SMOKE — اختيارات selector لمتعلم SIM قبل/بعد

`selector.py --sim` (20 خطوة، كون 40 ذرة من 984+767): **الأثر مطابق تماماً** —
نفس المختار في كل خطوة (`sim_trace.json` لم يتغير؛ checks متطابقة:
`ordered_new=2, due_first_violations=0, jump_violations=0, empty_steps=1`).

لأن الحواف الواردة إلى الكون الـ40 تغيّرت قليلاً، قسناً أيضاً قائمة المحرك الكاملة
على بذرة المتعلم (`data/sim_learner.json`، limit=كل الذرات):

| المقياس | قبل | بعد |
|---|---|---|
| طول القائمة المرتبة | 49 | 50 |
| مجموعة due (الأول 12) | مطابقة ترتيباً ومحتوى | — |
| حواف graph.edges واردة إلى كون المحاكاة | 40 (35 seq + 3 cross + 2 tag) | 38 (35 seq + 3 cross) |

**أين تغيّر الترتيب فعلياً** (مجموعة «new» فقط؛ due لم تتبدل):

| الذرة | قبل | بعد | السبب |
|---|---|---|---|
| `jamic_ulum|1` | في الجبهة | خرجت | كسبت سابقة tag_chain غير مُتقنة (المادة غير مصنّفة لكن وسومها تشارك مجموعات علّيها صعوبة حقيقية) |
| `riyad_salihin_dalil|1` | في الجبهة | خرجت | كذلك — سابقة tag_chain جديدة غير مُتقنة |
| `1007_إقبار الانتصار|1007-0035` | محجوبة | دخلت الجبهة | فقدت سابقة tag_chain التي كانت تشير إليها (إعادة تجميع R4) |
| `779_علم البلاغة الأول|779-001-e01` | محجوبة | دخلت الجبهة | كذلك |
| `787_دورة النحو الثاني|787-001-e01` | محجوبة | دخلت الجبهة | كذلك |
| 34 ذرة أخرى | — | — | إزاحات ±1–3 مواضع نتيجة دخول/خروج الجبهة |

داخل نافذة الـ40 ذرة: فقد `984|001.04` السابقة `790|022.03` و`984|002.09` فقدت
`793|008.02` (tag_chain) — لا أثر على الاختيارات لأن سابقات خارج الكون لا تدخل
بوابة المحاكاة أصلاً. حافة kind_anchor الناجية (`767|022.02→022.04`) خارج
النافذة (ملف 022)، فلم تغيّر أثر الـ20 خطوة.

## ملاحظة تقنية — إصلاحان في selector.py

- `build_edges` كان يرتّب بـ`char_start` وحده بينما build_graph يكسّر التعادل
  بـ`(char_start, char_end, id)` — تعادلات char_start في سلاسل التفاسير
  ودليل الفالحين (~3,970 تعادلاً) كانت تولّد حواف R1 معكوسة ⇒ دورات ثنائية
  ضد ملف الحواف المولّد وتحطّم `--sim`. طُبّق نفس المفتاح.
- رسالة الدورات المسقطة كانت تقرأ `d['from']` بينما السجل يخزّن `atom_id` —
  KeyError. صُحّحت.
