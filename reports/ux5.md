# ux5 — نقد UX: prog/*.html

CDP حي لـindex + مراجعة مصدرية main للـpending · 2026-09-25.

## prog/index.html — «برمج»: يعمل جيداً (حي)

- 360px نظيف: صفر فيض، صفر عناصر خارجية، خط ≥11px، 0 أخطاء JS.
- تسلسل جيد: H1 «برمج» → عنوان الدرس → «أحسنت! أتممت» → بطاقات المفاهيم → اختبار قصير → حقل stdin — مسار تعلّم مرتّب منطقياً.
- docH 1141 (~1.3 شاشة) — كثافة معتدلة، لا إغراق.
- 2 أهداف لمس مرئية فقط — spartan لكن صالح؛ أهداف ≥34px. درجة: 8/10.

## prog/reference.html — المرجع السريع (pending — مصدر فقط)

- بحث مدمج («ابحث في البنود…») — النمط الصحيح لمرجع بنود كثيرة؛ التقييم الحي ينتظر النشر.

## prog/debugger.html — المُنقِّح (pending — مصدر فقط)

- عنوان واضح + grid — أداة تنقيح؛ يحتاج قياساً حيّاً بعد النشر.

## prog/playground.html — المُنشِئ (pending — مصدر فقط)

- حقل تسمية تجربة + grid — ساحة تجربة؛ التقييم الحي ينتظر.

## ملاحظة

reference/debugger/playground/roadmap/patterns/refactor: **roadmap+patterns+refactor غائبون عن main أصلاً**؛ الـ3 موجودون مصدرياً وينتظرون النشر — لا تسليم UX حيّ ممكن قبل هبوط بناء.
